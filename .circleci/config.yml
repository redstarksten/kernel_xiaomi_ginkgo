version: 2.1
jobs:
  compile:
   docker:
      - image: ubuntu:18.04
   steps:
      - run:
          command: |
           apt-get -y update && apt-get -y upgrade && apt-get -y install git ccache automake lzop bison gperf build-essential zip curl zlib1g-dev g++-multilib libxml2-utils bzip2 libbz2-dev libbz2-1.0 libghc-bzlib-dev squashfs-tools pngcrush schedtool dpkg-dev liblz4-tool make optipng
           git config --global user.name "redstarksten"
           git config --global user.email "mahadewanto2@gmail.com"
           git config --global color.ui false
           git clone https://github.com/NusantaraDevs/clang
           git clone https://github.com/redstarksten/kernel_xiaomi_ginkgo
           cd kernel_xiaomi_ginkgo
           bash build.sh
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
      
      
